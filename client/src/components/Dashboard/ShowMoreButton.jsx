const ShowMoreButton = ({ onClick }) => (
  <button
    onClick={onClick}
    className="w-full self-center py-7 text-sm text-orange-500"
  >
    Show more
  </button>
)

export default ShowMoreButton
